For setup:
1. Forked the repository delveccj/ForensicsDetective on GitHub.
2. Opened the fork in GitHub Codespaces.
3. Verified Python environment (Python 3.12).
4. Installed required packages:

   pip install numpy pandas scikit-learn pillow opencv-python matplotlib seaborn

5. Verified repository functionality by running:

   python train_baseline_classifiers.py
   python train_3class_classifiers.py

Both scripts executed successfully and trained classifiers using the dataset contained in:
- word_pdfs_png
- google_docs_pdfs_png
- python_pdfs_png